# Handoff Snapshot

## Metadata
- Created at: 2026-05-23 15:13:02 +0900
- Repo root: `/home/[USER]/simple-vibe-ide`
- Branch: main
- Commit: 0153880
- Mode: Save
- Agent: codex
- Git dirty: yes; ongoing performance optimization diff is uncommitted

## Project Goal
- Optimize the Windows-built Simple Vibe IDE for perceived performance while preserving all current features.
- Focus areas: lag-free Explorer scrolling, lighter Browser used/unused states, cache behavior, terminal/background work, workspace switching, and active tab/control updates without visible stutter.
- Do not mark the goal complete until Windows/Rust validation and real Windows runtime smoke tests confirm behavior.

## Current State
- Done:
  - Broad frontend performance stack remains in the worktree: Explorer virtual rows/cache prune/idle prefetch and watcher gating, workspace restore first-paint yields/control no-op guards, targeted workspace/widget tab activation, terminal lazy xterm runtime/chunked writes/bounded scan buffers/fit and active-state scoping, Browser iframe/console suspend/hidden queue batching/active-frame sizing, and Rust/backend directory/listing optimizations.
  - Recent patches present in `src/main.ts`: Browser console local-port scan exec loop, Terminal OSC7 cwd scan exec loop, Terminal tilde cwd expansion allocation trim, Panel interaction DOM query trim, Explorer row render signature precompute, Explorer visible lookup map trim, Workspace restore tab loop helpers, Workspace image store loop compaction, Workspace snapshot clone loop helpers, Browser console runtime tail copy loops, Browser console batch append loop, Browser console hidden single-payload queue, Browser console local-port scan drain loop, Explorer directory cache idle prune, Explorer directory cache key reuse, Browser tab direct DOM render, Browser console compact object formatter, Browser preview proxy local-port lookup, Browser tab index lookup cache, Editor/Image/Notes tab index lookup cache, Workspace snapshot index lookup cache, Workspace tab child lookup cache, Editor/Image/Notes tab render allocation trim, Terminal widget tab render allocation trim, Browser forward row render allocation trim, Browser frame iteration allocation trim, Explorer row child lookup cache, Browser console append index cache, Browser console single append fragment skip, Workspace tab direct DOM render, Browser tab same-order allocation trim, Market ticker render allocation trim, Terminal widget card direct DOM render, Explorer watcher polling allocation trim, Image history render allocation trim, Export progress render allocation trim, Export job state update allocation trim, and Rust/Tauri validation unblock.
  - Latest patch: Rust/Tauri validation unblock. WSL Rust toolchain validation now works via `$HOME/.cargo/bin`; `windows` crate dependency is target-gated to Windows, `src-tauri/icons/icon.png` was added for Tauri context generation, and `npm run tauri -- build --no-bundle` now passes in WSL.
  - `codex.md` has privacy-safe patch notes through the Rust/Tauri validation unblock patch.
- In progress:
  - Validate the full dirty performance diff in the actual Windows app with large WSL/SSH workspaces, long Explorer directories, noisy hidden terminals, many Browser tabs/proxy tabs/logs, many Editor/Image/Notes tabs, manual/auto forwards, many profiles, frequent workspace switches, workspace tab reorders, Browser device changes, Explorer typeahead/selection/rename, and tab/control operations.
- Broken / incomplete:
  - Windows runtime smoke testing is still required; WSL/Linux no-bundle Tauri build now passes, but the final Windows app behavior has not been smoked yet.

## Files Touched
- `src/main.ts`: main performance work, including Export job state update allocation trim, Export progress render allocation trim, Image history render allocation trim, Explorer watcher polling allocation trim, Terminal widget card direct DOM render, Market ticker render allocation trim, Browser tab same-order allocation trim, Workspace tab direct DOM render, Browser console single append fragment skip, Browser console append index cache, Explorer row child lookup cache, Browser frame iteration allocation trim, Browser forward row render allocation trim, Terminal widget tab render allocation trim, Editor/Image/Notes render allocation trim, Workspace tab child lookup cache, Workspace snapshot index lookup cache, Editor/Image/Notes tab index lookup cache, Browser tab index lookup cache, Browser preview proxy local-port lookup, Browser console compact object formatter, Browser tab direct DOM render, Explorer directory cache key reuse, Explorer directory cache idle/batched prune, Browser console local-port scan drain loop, hidden Browser console single-payload queue optimization, Browser console batch append/prune optimization, Browser console runtime tail copy loops, workspace snapshot/image/restore loop helpers, Explorer/panel/terminal/Browser lookup and allocation optimizations, and earlier Explorer/workspace/terminal/Browser responsiveness changes.
- `src-tauri/src/lib.rs`: Rust backend performance changes for directory/signature/proxy/console bridge paths; formatted with Rust 2021 edition and included in passing WSL Tauri no-bundle build.
- `src-tauri/Cargo.toml`: `windows` crate moved to Windows-only target dependencies so WSL/Linux validation does not compile Windows-only bindings.
- `src-tauri/icons/icon.png`: PNG icon generated from the existing ICO to satisfy Tauri context generation in WSL/Linux builds.
- `src/api.ts`, `src/types.ts`: shared type/API updates for backend changes.
- `src/styles.css`: Explorer/Browser/widget performance-related style changes.
- `codex.md`: privacy-safe patch notes and verification notes.
- `.handoff/latest.md`, `.handoff/YYYY-MM-DD-HHMMSS-session.md`: repo-local handoff snapshots.

## Important Decisions
- Favor responsiveness over aggressive background freshness. Defer hidden/background/prefetch/watch/cache-prune/snapshot/console-replay/terminal-write work behind UI-idle and input-pending guards where behavior remains acceptable.
- Preserve terminal, Browser, Explorer, Editor, Image, Notes, forwarding, profile, and workspace-tab behavior while avoiding large synchronous string concatenation, repeated scans, repeated workspace snapshot index scans, repeated workspace tab child DOM queries, workspace tab `innerHTML` parse/query cycles, same-order workspace, Browser tab, and Market ticker prune/set allocation, repeated directory-cache key construction, repeated Browser/Editor/Image/Notes tab index scans, repeated DOM queries, Browser tab `innerHTML` parse/query cycles, Editor/Image/Notes tab `innerHTML` parse/query cycles, repeated Market ticker child DOM queries, same-order Editor/Image/Notes, terminal widget tab, and Browser forward row prune/set allocation, Browser forward row child queries, Browser iframe lifecycle frame-array allocation, Explorer row live-children lookups, Terminal widget card `innerHTML` parse/query cycles, Terminal widget tab `innerHTML` parse/query cycles, one-element arrays on hidden Browser console records, hidden-console compact object key arrays, repeated Browser console tail scans and single-append fragment allocation on noisy append, preview-proxy local-port fallback scans in common paths, intermediate arrays, spread pushes on noisy batches, temporary splice batch arrays for hidden Browser console local-port scans, synchronous all-entry Explorer directory cache prune, per-poll Explorer watcher `Set`/`Map` allocation, image-history same-order prune/query allocation, export progress row rebuild/prune allocation, export job scan/spread/slice allocation, and large one-shot terminal writes.
- Workspace snapshots, workspace tab child refs, active/open-existing Browser, Browser tab indexes, Editor/Image/Notes tab indexes, preview-proxy, preview-proxy local-port, device preset, terminal-widget, Editor, Image, Notes, forward, detected-port, workspace-snapshot, connection-profile, and Explorer-entry lookups should be O(1) in common cases with fallback lookup for robustness.
- Explorer cache pruning should defer while scrolling/input is active, with bounded hard-limit pruning only if cache growth exceeds busy ceilings.
- Browser console visible state should reuse DOM where possible and keep hidden-log replay, hidden queue pruning, and hidden local-port scans deferred/batched so Browser tabs do not feel heavy when unused.
- Prioritize workspace first paint/first interaction; delayed snapshot persistence is acceptable if existing save/switch flush paths remain intact.
- Actual repo state remains source of truth over old handoff snapshots.

## Known Issues / Errors
- Risk: Rust/Tauri validation unblock must preserve Windows capture-protection builds while allowing WSL/Linux validation to skip Windows-only bindings.
  - Mitigation: only the dependency location changed to `target.'cfg(windows)'.dependencies`; the Rust source already guarded Windows API imports/functions with `#[cfg(windows)]`; WSL no-bundle Tauri build passed, and Windows runtime smoke testing remains required.
- Risk: Export job state update allocation trim must preserve repeated export progress updates, cancelled/failed/completed transitions, list trimming, workspace clearing, and completed export drag-out/open actions.
  - Mitigation: the id lookup is cleared with workspace panels, trimmed jobs delete lookup entries, action rows resolve the current job from the lookup at event time, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Export progress render allocation trim must preserve background export progress, cancel, completed export drag-out/open actions, export list reorder/prune, and UI responsiveness while Explorer/Browser/terminal work is busy.
  - Mitigation: same-order skip is gated by unchanged order signature and child count, stale-cache pruning remains on order/length rebuilds, action buttons rebuild only when action mode changes and resolve current row/job at event time, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Image history render allocation trim must preserve image paste history, history preview, pasting image tags into the active terminal, clearing history, workspace restore, and frequent image panel open/close.
  - Mitigation: same-order skip is gated by unchanged order signature and child count, stale-cache pruning remains on order/length rebuilds, empty history clears the cache directly, child refs are cached from the same row children with fallback lookup, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Explorer watcher polling allocation trim must preserve background refresh, expanded directory polling, stale signature detection, scroll/input pause behavior, workspace switching, and WSL/SSH large-folder interaction.
  - Mitigation: watched path order/limits remain the same, duplicate suppression still uses normalized path keys, signature result lookup still matches normalized paths, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Market ticker render allocation trim must preserve live quote refresh, add/remove custom symbols, persisted custom symbol restore, and UI updates while Explorer/Browser/terminal work is busy.
  - Mitigation: same-order skip is gated by unchanged order signature and child count, stale-cache pruning remains on order/length rebuilds, child refs are cached from the same chip children with fallback lookup, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Terminal widget card direct DOM render must preserve terminal widget creation, card drag/resize, new shell tabs, close widget, workspace restore/rebuild, and active prompt targeting.
  - Mitigation: direct DOM creation preserves the same classes/titles/aria labels/text and event handlers are wired to the same behaviors through direct refs, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Browser tab same-order allocation trim must preserve Browser tab open, activate, close active/inactive, title/URL updates, duplicate URLs, workspace restore, hidden/visible Browser panel transitions, and many-tab switching.
  - Mitigation: same-order skip is gated by unchanged order signature and child count, stale-cache pruning remains on order/length rebuilds, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Workspace tab direct DOM render must preserve workspace tab activation, copy, close, capture-protection toggle, SVG icons, drag/reorder, restore, and frequent workspace switching.
  - Mitigation: direct DOM creation preserves the same button classes/type/title/aria/text and SVG attributes, event handlers still read current dataset values, same-order skip is gated by unchanged order signature and child count, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Browser console single append fragment skip must preserve single-log visible append, multi-log batch append, console clear, trimming, hidden console replay, and noisy Browser console output.
  - Mitigation: direct append is used only when exactly one new log is detected, the existing fragment path remains for multi-log batches and replacement, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Browser console append index cache must preserve visible console append, console clear, hidden console replay, log trimming, workspace restore, Browser panel hide/show, and noisy Browser console output.
  - Mitigation: cached index is validated against the last rendered log id before use, trim/clear paths reset or shift it, fallback tail id scanning remains, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Explorer row child lookup cache must preserve large-directory scrolling, selection, drop-target highlight, inline rename, loading rows, file-size toggle, expanded/collapsed directories, row recycling, and workspace switching.
  - Mitigation: cached child refs are created with the same row children and fallback to the current row children when needed, row static/state signatures are unchanged, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Browser frame iteration allocation trim must preserve Browser tab switching, workspace switching, inactive-frame suspension, frame restore/load, console capture-detail sync, Browser panel hide/show, and clearing Browser frames.
  - Mitigation: iteration still uses the existing frame indexes and prunes disconnected frames, active-workspace checks and frame hide/suspend/clear operations are unchanged in intent, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Browser forward row render allocation trim must preserve detected-port rows, manual forwards, auto-forwarded Browser opens, stop/ignore actions, proxy-local-port filtering, hidden/visible Browser panel transitions, and workspace switching.
  - Mitigation: cached child refs are attached to the same row elements, event handlers still read current dataset values, empty rows clear cached parts, same-order skip is gated by unchanged order signature and child count, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Terminal widget tab render allocation trim must preserve terminal tab activation, close active/inactive, focus handoff, title/command updates, terminal widget switching, hidden output buffering, workspace restore/rebuild, and frequent workspace switching.
  - Mitigation: direct DOM creation preserves the same button classes/type/title/aria/text, event handlers still read the current dataset pane id, update signatures are unchanged, same-order skip is gated by unchanged order signature and child count, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Editor/Image/Notes tab render allocation trim must preserve tab open, activate, close active/inactive, titles, active classes, note theme classes, same-order refresh correctness, note hydration/save timers, image source/history updates, workspace restore/rebuild, and frequent workspace switching.
  - Mitigation: direct DOM creation preserves the same button classes/type/title/aria/text, event handlers still read the current dataset id, update signatures are unchanged, same-order skip is gated by unchanged order signature and child count, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Workspace tab child lookup cache must preserve workspace tab activation, copy, close, capture-protection toggle, drag/reorder, restore, and frequent workspace switching.
  - Mitigation: existing button classes/listeners are preserved and cached refs are created from the same elements; TypeScript/build validation passed; Windows runtime smoke testing remains required.
- Risk: Prior workspace/Explorer/panel/terminal/Browser lookup and allocation patches must preserve edge cases under real interaction.
  - Mitigation: TypeScript/build validation passed; Windows runtime smoke testing remains required.

## Next Actions
1. Run final Windows environment validation/smoke: Windows `npm run tauri -- build --no-bundle` or full bundle build, then launch the Windows-built app and test runtime behavior.
2. Smoke test terminal tab/widget behavior: activate terminal tabs, close active/inactive tabs, verify focus handoff, title/command updates, terminal widget switching, hidden output buffering, workspace restore/rebuild, and frequent workspace switching.
3. Smoke test Editor/Image/Notes many-tab behavior: open many tabs, activate existing tabs, close active/inactive tabs, duplicate paths/source paths, note hydration/save timers, image history/source changes, workspace restore/rebuild, and frequent workspace switching.
4. Smoke test workspace tabs/lifecycle: activation, copy, close active/inactive, capture-protection toggle, SVG icons, drag/reorder, create/copy persistence, restore, and frequent workspace switching.
5. Smoke test Browser forward/detected-port behavior: detected-port rows, manual forwards, auto-forwarded Browser opens, stop/ignore actions, proxy-local-port filtering, hidden/visible Browser panel transitions, and workspace switching.
6. Smoke test Browser frame lifecycle behavior: Browser tab switching, workspace switching, inactive-frame suspension, frame restore/load, console capture-detail sync, Browser panel hide/show, and clearing Browser frames.
7. Smoke test Browser console append behavior: single-log visible append, multi-log batch append, console clear, trimming, hidden console replay, workspace restore, Browser panel hide/show, and noisy Browser console output.
8. Smoke test Browser console index behavior: cached last-rendered index after visible append, console clear, hidden console replay, log trimming, workspace restore, Browser panel hide/show, and noisy Browser console output.
9. Smoke test Browser many-tab behavior: open many tabs, activate existing tabs, close active/inactive tabs, title/URL updates, duplicate URLs, workspace restore/rebuild, hidden/visible Browser panel transitions, console visible/hidden states, and many-tab switching.
10. Smoke test preview proxy/Browser forwarding behavior: proxy reuse, stale proxy removal, local-port auto-forward skip logic, manual/auto forwards, Browser tab open/activate through proxy, workspace switch away/back, and many proxy tabs/logs.
11. Smoke test Explorer row render behavior: large-directory scrolling, selection, drop-target highlight, inline rename, loading rows, file-size toggle, expanded/collapsed directories, row recycling, and workspace switching.
12. Smoke test Explorer cache/scroll behavior: large-directory fast scrolling, visible-directory prefetch, cache reuse and expiry, cache invalidation with/without file sizes, refresh, rename, delete, workspace switch away/back, typeahead/selection, and expanded/collapsed trees.
13. Smoke test terminal cwd/preview detection behavior: tilde prompt cwd values, OSC7 cwd sequences, PowerShell/bash prompt cwd, URL/listening/running/server text patterns, duplicate suppression, detected-port rows, manual/auto Browser open flow, hidden/inactive output, and workspace switching.
14. If lag remains, profile the real Windows runtime to separate Explorer DOM/cache-prune/backend polling/path helpers/typeahead/entry lookup, terminal write/scan/fit/layout/active-state/cwd parsing, Browser iframe/console/frame/sizing/device/tab/proxy/forward lookup, workspace-store/profile persistence, and tab/control render work.

## Commands
```bash
export PATH="$HOME/.cargo/bin:$PATH"
rustfmt --edition 2021 src-tauri/src/lib.rs
cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check
npm run tauri -- build --no-bundle
npm run check
npm run build
git diff --check
```

## Last Test Result
- Command: `PATH="$HOME/.cargo/bin:$PATH" rustfmt --edition 2021 src-tauri/src/lib.rs`
  - Result: pass.
- Command: `cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check`
  - Result: pass.
- Command: `npm run tauri -- build --no-bundle`
  - Result: pass in WSL/Linux; built `src-tauri/target/release/simple-vibe-ide`.
- Command: `npm run check`
  - Result: pass.
- Command: `npm run build`
  - Result: pass; main app chunk 273.94 kB minified, CSS 40.13 kB minified, xterm lazy chunk 289.71 kB minified.
- Command: `git diff --check`
  - Result: pass.

## Constraints
- Do not revert unrelated or user changes.
- Preserve all existing features while optimizing perceived performance.
- Keep Windows-first runtime behavior.
- Avoid committing secrets, tokens, raw local usernames, private URLs, or real home paths.
- Keep docs and handoff snapshots compact and privacy-safe.

## Resume Instructions
- Start by reading this file and `codex.md`.
- Verify actual repo state before editing: `git status --short`, `git diff --stat`, and relevant functions in `src/main.ts`.
- Trust repo state over this snapshot if they differ.
- Continue from `Next Actions`; do not mark the goal complete until Windows runtime behavior is validated.

## Unknowns
- Whether the full optimization stack eliminates perceived lag in the actual Windows-built app.
- Whether Rust/Tauri validation unblock preserves every Windows capture-protection/build edge case in the actual Windows target.
- Whether Export job state update allocation trim preserves every repeated-progress/transition/trim/clear/drag-open edge case.
- Whether Export progress render allocation trim preserves every progress/cancel/drag-open/reorder/prune edge case.
- Whether Image history render allocation trim preserves every paste-history/preview/paste-tag/clear/restore/panel-toggle edge case.
- Whether Explorer watcher polling allocation trim preserves every background refresh/signature/expanded-dir/pause/workspace-switch edge case.
- Whether Terminal widget card direct DOM render preserves every terminal widget/create/drag/resize/new-tab/close/restore edge case.
- Whether Market ticker render allocation trim preserves every live quote/add/remove/persisted-custom edge case.
- Whether Browser tab same-order allocation trim preserves every many-tab/title/restore edge case.
- Whether Workspace tab direct DOM render preserves every workspace tab/icon/reorder edge case.
- Whether Browser console single append fragment skip preserves every single/multi/hidden/trim console append edge case.
- Whether Browser console append index cache preserves every visible/hidden/trim/restore console edge case.
- Whether Explorer row child lookup cache preserves every visible-row/rename/loading/recycle edge case.
- Whether Browser frame iteration allocation trim preserves every Browser frame lifecycle/workspace-switch edge case.
- Whether Browser forward row render allocation trim preserves every detected-port/forward/proxy/workspace edge case.
- Whether Terminal widget tab render allocation trim preserves every terminal tab/widget/focus/restore edge case.
- Whether Editor/Image/Notes tab render allocation trim preserves every many-tab/restored-tab edge case.
- Whether Workspace tab child lookup cache preserves every tab/control/reorder edge case.
- Whether Workspace snapshot index lookup cache preserves every workspace lifecycle/reorder/persistence edge case.
- Whether Editor/Image/Notes tab index lookup caches preserve every many-tab/duplicate/restored tab edge case.
- Whether Browser tab index lookup cache preserves every many-tab/duplicate/restored tab edge case.
- Whether Browser preview proxy local-port lookup preserves every proxy/forward/workspace edge case.
- Whether Explorer directory cache key reuse and idle prune preserve every large-scroll/prefetch/cache/refresh/mutation edge case.
- Whether prior workspace snapshot/image/restore, Explorer lookup/render, panel interaction, terminal cwd/scan, Browser lookup/console, and Rust backend patches preserve every runtime edge case under real interaction.
