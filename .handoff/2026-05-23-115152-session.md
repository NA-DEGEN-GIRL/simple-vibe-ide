# Handoff Snapshot

## Metadata
- Created at: 2026-05-23 11:51:52 +0900
- Repo root: `/home/[USER]/simple-vibe-ide`
- Branch: main
- Commit: 0153880
- Mode: Save
- Agent: codex
- Git dirty: yes; ongoing performance optimization diff is uncommitted

## Project Goal
- Optimize the Windows-built Simple Vibe IDE for perceived performance while preserving all current features.
- Focus areas: lag-free Explorer scrolling, lighter Browser used/unused states, cache behavior, terminal/background work, workspace switching, and active tab/control updates without visible stutter.
- Do not mark the goal complete until Windows/Rust validation and real Windows runtime smoke tests confirm behavior.

## Current State
- Done:
  - Broad frontend performance stack remains in the worktree: Explorer virtual rows/cache prune/idle prefetch and watcher gating, workspace restore first-paint yields/control no-op guards, targeted workspace/widget tab activation, terminal lazy xterm runtime/chunked writes/bounded scan buffers/fit and active-state scoping, Browser iframe/console suspend/hidden queue batching/active-frame sizing, and Rust/backend directory/listing optimizations.
  - Latest workspace persistence patch is present in `src/main.ts`: store persistence reuses the existing snapshot array when under the persisted limit, store signature generation uses a bounded loop instead of slice/map/join, snapshot signature generation concatenates directly instead of allocating a large temporary array before `join('|')`, and persistence-triggered workspace tab rendering runs only if visible tab signature/order/count changed.
  - `codex.md` now has a privacy-safe patch note for the workspace persistence signature allocation guards.
- In progress:
  - Validate the full dirty performance diff in the actual Windows app with large WSL/SSH workspaces, long Explorer directories, noisy hidden terminals, Browser tabs/logs, frequent workspace switches, and tab/control operations.
- Broken / incomplete:
  - This WSL shell has no `rustfmt` or `cargo`, so Rust formatting and Tauri no-bundle build cannot be completed here.
  - Windows runtime smoke testing is still required; frontend/static validation alone is not enough to complete the goal.

## Files Touched
- `src/main.ts`: main performance work, including latest workspace persistence signature allocation/render guards plus Explorer, workspace restore, terminal, Browser, and tab activation optimizations.
- `src-tauri/src/lib.rs`: Rust backend performance changes for directory/signature/proxy/console bridge paths; needs Rust toolchain validation.
- `src/api.ts`, `src/types.ts`: shared type/API updates for backend changes.
- `src/styles.css`: Explorer/Browser/widget performance-related style changes.
- `codex.md`: privacy-safe patch notes and verification notes.
- `.handoff/latest.md`, `.handoff/YYYY-MM-DD-HHMMSS-session.md`: repo-local handoff snapshots.

## Important Decisions
- Favor responsiveness over aggressive background freshness. Defer hidden/background/prefetch/watch/cache-prune/snapshot/console-replay/terminal-write work behind UI-idle and input-pending guards where behavior remains acceptable.
- Preserve terminal output and detection behavior, but avoid large synchronous string concatenation and large one-shot `xterm.write` bursts.
- Distinguish Browser user-visible immediate work from hidden/unused console/frame work.
- Prioritize workspace first paint/first interaction; delayed snapshot persistence is acceptable if existing save/switch flush paths remain intact.
- Workspace persistence JSON/signature work should avoid avoidable allocation, and snapshot-only saves should not enter workspace tab rendering when visible tab display did not change.
- Actual repo state remains source of truth over old handoff snapshots.

## Known Issues / Errors
- Error: `rustfmt src-tauri/src/lib.rs` fails in this WSL shell.
  - Exact result: `/bin/bash: line 5: rustfmt: command not found` / exit 127.
- Error: `npm run tauri -- build --no-bundle` fails in this WSL shell.
  - Exact result: `failed to run 'cargo metadata' ... No such file or directory (os error 2)`.
- Risk: workspace-store persistence now avoids a workspace-tab render unless tab signature/order/count changed.
  - Mitigation: full render still runs for structural tab changes and fallback paths; verify workspace tab create/copy/close/reorder/capture-protection and snapshot persistence after restart.
- Risk: workspace snapshot signatures now concatenate directly.
  - Mitigation: delimiter order and signature fields are preserved; frontend type/build validation passed, but verify persistence across restarts in the Windows app.
- Risk: terminal scan buffers keep bounded tails before append.
  - Mitigation: scan limits are unchanged; verify local-port and prompt cwd detection under large output.
- Risk: large terminal output backlogs now flush over multiple frames/timers.
  - Mitigation: output order is preserved; verify visible catch-up and hidden backlog activation in Windows app.
- Risk: Browser hidden console payload replay is deferred during visible layout restore.
  - Mitigation: explicit user console show still flushes immediately; verify hidden-console replay and panel show/hide.
- Risk: Explorer prefetch/watch/cache-prune is less eager during busy UI periods.
  - Mitigation: explicit expand/open/manual refresh paths still load directly; verify watcher freshness and long-directory scrolling.

## Next Actions
1. Run Windows/Rust validation from the Windows environment: `rustfmt src-tauri/src/lib.rs` and `npm run tauri -- build --no-bundle`.
2. Smoke test workspace persistence behavior: rapid workspace switching, immediate switch-away after restore, active workspace persistence after restart, snapshot-only saves while terminals/Browser tabs are active, and workspace tab create/copy/close/reorder/capture-protection.
3. Smoke test Explorer with large WSL and SSH workspaces: rapid scroll, very long directory scroll, expand/collapse, typeahead, selection, rename, drag/drop target, hover/select prefetch feel, watcher freshness, and file-size toggle.
4. Smoke test terminal behavior with noisy output: visible spam, hidden/background output catch-up, switching to a terminal with a large buffered backlog, local-port detection, prompt cwd detection, OSC7 cwd detection, shell tab activation, terminal close cleanup, and workspace switching with active background output.
5. Smoke test Browser behavior: panel show/hide, console show/hide/position changes, hidden-console log replay, iframe tab switching, opening existing URL, local proxy tabs, closing active/inactive tabs, desktop/device/rotate changes, preview URL updates, and workspace switching.
6. Smoke test workspace restore controls and widget tabs: profile/root controls, editor/image options, Notes opacity/theme/pin/status, panel visibility, Editor/Image/Notes tab switch/reselect/create/close/restore with many tabs.
7. If lag remains, profile the real Windows runtime to separate Explorer DOM/cache-prune/backend polling, terminal write/scan/fit/layout/active-state, Browser iframe/console/frame/sizing, workspace-store persistence, and tab/control render work.

## Commands
```bash
npm run check
npm run build
git diff --check
rustfmt src-tauri/src/lib.rs
npm run tauri -- build --no-bundle
```

## Last Test Result
- Command: `npm run check`
  - Result: pass.
- Command: `npm run build`
  - Result: pass; main app chunk 260.23 kB minified, CSS 40.13 kB minified, xterm lazy chunk 289.71 kB minified.
- Command: `git diff --check`
  - Result: pass.
- Command: `rustfmt src-tauri/src/lib.rs`
  - Result: blocked in WSL; `rustfmt` not found.
- Command: `npm run tauri -- build --no-bundle`
  - Result: blocked in WSL; `cargo` not installed, `cargo metadata` cannot run.

## Constraints
- Do not revert unrelated or user changes.
- Preserve all existing features while optimizing perceived performance.
- Keep Windows-first runtime behavior.
- Avoid committing secrets, tokens, raw local usernames, private URLs, or real home paths.
- Keep docs and handoff snapshots compact and privacy-safe.

## Resume Instructions
- Start by reading this file and `codex.md`.
- Verify actual repo state before editing: `git status --short`, `git diff --stat`, and relevant functions in `src/main.ts`.
- Trust repo state over this snapshot if they differ.
- Continue from `Next Actions`; do not mark the goal complete until Windows runtime behavior is validated.

## Unknowns
- Whether the full optimization stack eliminates perceived lag in the actual Windows-built app.
- Whether workspace persistence allocation/render guards cover all create/copy/close/reorder/capture-protection and restart persistence edge cases.
- Whether bounded terminal scan-buffer append keeps local-port and prompt-cwd detection reliable under large output.
- Whether chunked terminal write catch-up feels smooth enough under noisy terminal output.
- Whether hidden Browser console replay remains timely enough after restore while reducing first-interaction work.
- Whether Explorer watcher/prefetch freshness remains good enough now that background work pauses for pending input.
