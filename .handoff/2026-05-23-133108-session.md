# Handoff Snapshot

## Metadata
- Created at: 2026-05-23 13:31:08 +0900
- Repo root: `/home/[USER]/simple-vibe-ide`
- Branch: main
- Commit: 0153880
- Mode: Save
- Agent: codex
- Git dirty: yes; ongoing performance optimization diff is uncommitted

## Project Goal
- Optimize the Windows-built Simple Vibe IDE for perceived performance while preserving all current features.
- Focus areas: lag-free Explorer scrolling, lighter Browser used/unused states, cache behavior, terminal/background work, workspace switching, and active tab/control updates without visible stutter.
- Do not mark the goal complete until Windows/Rust validation and real Windows runtime smoke tests confirm behavior.

## Current State
- Done:
  - Broad frontend performance stack remains in the worktree: Explorer virtual rows/cache prune/idle prefetch and watcher gating, workspace restore first-paint yields/control no-op guards, targeted workspace/widget tab activation, terminal lazy xterm runtime/chunked writes/bounded scan buffers/fit and active-state scoping, Browser iframe/console suspend/hidden queue batching/active-frame sizing, and Rust/backend directory/listing optimizations.
  - Workspace persistence, UI signature, workspace-terminal snapshot, terminal-widget render, terminal-widget lookup, Browser-tab lookup, Browser preview-proxy lookup, Explorer basename/typeahead, Browser device-preset lookup, Editor/Image/Notes tab lookup, Forward/detected-port lookup, Browser console row reuse, Workspace snapshot lookup, Connection profile lookup, Explorer entry lookup, Terminal port scan allocation, Terminal prompt cwd tail-scan, Browser console local-port scan exec-loop, Terminal OSC7 cwd scan exec-loop, Terminal tilde cwd expansion allocation-trim, Panel interaction DOM query-trim, Explorer row render signature-precompute, Explorer visible lookup map-trim, Workspace restore tab loop-helper, and Workspace image store loop-compaction patches are present in `src/main.ts`.
  - Latest patch: Workspace image store loop compaction. Workspace store load, image-store parsing, and image tab/history compact/hydrate paths now use explicit loops instead of `filter().map()`, `Object.entries().filter()`, and nested `map()` chains.
  - `codex.md` now has privacy-safe patch notes through the Workspace image store loop-compaction patch.
- In progress:
  - Validate the full dirty performance diff in the actual Windows app with large WSL/SSH workspaces, long Explorer directories, noisy hidden terminals, many Browser tabs/proxy tabs/logs, many Editor/Image/Notes tabs, manual/auto forwards, many profiles, frequent workspace switches, workspace tab reorders, Browser device changes, Explorer typeahead/selection/rename, and tab/control operations.
- Broken / incomplete:
  - This WSL shell has no `rustfmt` or `cargo`, so Rust formatting and Tauri no-bundle build cannot be completed here.
  - Windows runtime smoke testing is still required; frontend/static validation alone is not enough to complete the goal.

## Files Touched
- `src/main.ts`: main performance work, including latest Workspace image store loop compaction plus Workspace restore tab loop helpers, Explorer visible lookup map trim, Explorer row render signature precompute, Panel interaction DOM query trim, Terminal tilde cwd expansion allocation trim, Terminal OSC7 cwd scan exec loop, Browser console local-port scan exec loop, terminal prompt cwd tail scan, terminal port scan allocation guard, Explorer entry lookup cache, connection profile lookup cache, workspace snapshot lookup cache, Browser console row reuse, Forward/detected-port lookup caches, Editor/Image/Notes tab lookup caches, Browser device preset lookup guard, Explorer path/typeahead guards, Browser preview proxy lookup caches, Browser tab lookup caches, terminal widget lookup caches, workspace terminal snapshot/restore allocation guards, terminal widget render/close allocation guards, UI signature allocation guards, workspace persistence allocation guards, and earlier Explorer/workspace/terminal/Browser optimizations.
- `src-tauri/src/lib.rs`: Rust backend performance changes for directory/signature/proxy/console bridge paths; needs Rust toolchain validation.
- `src/api.ts`, `src/types.ts`: shared type/API updates for backend changes.
- `src/styles.css`: Explorer/Browser/widget performance-related style changes.
- `codex.md`: privacy-safe patch notes and verification notes.
- `.handoff/latest.md`, `.handoff/YYYY-MM-DD-HHMMSS-session.md`: repo-local handoff snapshots.

## Important Decisions
- Favor responsiveness over aggressive background freshness. Defer hidden/background/prefetch/watch/cache-prune/snapshot/console-replay/terminal-write work behind UI-idle and input-pending guards where behavior remains acceptable.
- Preserve terminal, Browser, Explorer, Editor, Image, Notes, forwarding, profile, and workspace-tab behavior, but avoid large synchronous string concatenation, filtered arrays, repeated tab/widget/proxy/device/forward/workspace/profile/Explorer-tree scans, Browser console row churn, terminal port-scan intermediate arrays, terminal prompt-cwd split/slice arrays, workspace image store compact/hydrate map/filter allocation, workspace restore tab map/filter allocation, Explorer visible lookup map churn, Explorer row signature/file-size/disclosure recomputation, panel interaction DOM query/allocation, Terminal tilde cwd `filter()`/`split()` allocation, Terminal OSC7 `matchAll` allocation, Browser console local-port `matchAll` allocation, path split/filter/pop arrays, locale lowercasing in Explorer typeahead, and large one-shot `xterm.write` bursts on activation/switch/render/snapshot paths.
- Active/open-existing Browser, preview-proxy, device preset, terminal-widget, Editor, Image, Notes, forward, detected-port, workspace-snapshot, connection-profile, and Explorer-entry lookups should be O(1) in common cases with fallback linear/recursive lookup for robustness.
- Browser console visible state should reuse DOM where possible and keep hidden-log replay and hidden local-port scans deferred so Browser tabs do not feel heavy when unused.
- Prioritize workspace first paint/first interaction; delayed snapshot persistence is acceptable if existing save/switch flush paths remain intact.
- Actual repo state remains source of truth over old handoff snapshots.

## Known Issues / Errors
- Error: `rustfmt src-tauri/src/lib.rs` fails in this WSL shell.
  - Exact result: `/bin/bash: line 1: rustfmt: command not found` / exit 127.
- Error: `npm run tauri -- build --no-bundle` fails in this WSL shell.
  - Exact result: `failed to run 'cargo metadata' ... No such file or directory (os error 2)`.
- Risk: Workspace image store loop compaction must preserve workspace store load/save, image tab/history data refs, copied workspace image refs, and image history hydration.
  - Mitigation: loop helpers construct the same snapshot shapes, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Workspace restore tab loop helpers must preserve Editor/Image/Notes/Browser tab restore order, active-tab fallback, preview-proxy restore, hidden-panel behavior, and runtime-cache restore behavior.
  - Mitigation: loop helpers construct the same runtime state shape, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Explorer visible lookup map trim must preserve selection, row lookup, drag/drop target detection, typeahead cache, and fallback entry lookup behavior.
  - Mitigation: visible lookups now route through the same path-key index map, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Explorer row render signature precompute must preserve expanded directory disclosure text, file-size rendering, inline rename rows, and new-file/new-folder duplicate-name behavior.
  - Mitigation: precomputed values are rebuilt with the visible-row list, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Panel interaction DOM query trim must preserve panel focus highlighting, keyboard resize target changes, drag/resize snapping, workspace open/close button states, and LLM launch button bindings.
  - Mitigation: cached element lists are initialized after static DOM creation, known panel/widget caches are used, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Terminal tilde cwd expansion allocation trim must preserve cwd resolution for `~` and `~/...` prompt paths.
  - Mitigation: candidate priority is unchanged, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Terminal OSC7 cwd scan exec loop must preserve last-OSC7-wins cwd behavior across `BEL` and `ESC \\` terminated OSC7 sequences.
  - Mitigation: same regex semantics are used, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Browser console local-port scan exec loop must preserve `http`, `https`, `ws`, and `wss` localhost dependency auto-forward behavior.
  - Mitigation: same URL pattern semantics are used, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Terminal prompt cwd tail scan must preserve PowerShell and POSIX prompt detection over recent output.
  - Mitigation: the same regex patterns and 40-line scan window are used, TypeScript/build validation passed, and Windows runtime smoke testing remains required.
- Risk: Browser/terminal/workspace lookup and allocation patches must preserve edge cases under real interaction.
  - Mitigation: TypeScript/build validation passed; Windows runtime smoke testing remains required.

## Next Actions
1. Run Windows/Rust validation from the Windows environment: `rustfmt src-tauri/src/lib.rs` and `npm run tauri -- build --no-bundle`.
2. Smoke test workspace store/image persistence: workspace load/save, image tab/history restore, image data-ref persistence, copied workspaces with images, and workspace switching.
3. Smoke test workspace restore/switch behavior: Editor tabs, Image tabs/history, Notes tabs/hydration, Browser tabs, active-tab fallback, preview proxies, hidden panels, runtime-cache restores, and frequent workspace switching.
4. Smoke test Explorer row/lookup behavior: fast scrolling, selection movement, row lookup, drag/drop target detection, typeahead, expanded/collapsed disclosure arrows, file-size toggle, inline rename, new file/folder duplicate names in large directories, and workspace switching.
5. Smoke test panel interactions: focus highlighting, keyboard resizing, drag/resize snapping against panels/terminal widgets/grid, workspace open/close button disabled states, panel toggle buttons, and LLM launcher buttons.
6. Smoke test terminal cwd behavior: tilde prompt cwd values, OSC7 cwd sequences, PowerShell prompt cwd, bash prompt cwd, manual `cd` tracking, hidden/inactive terminal output, and workspace switching with noisy terminals.
7. Smoke test Browser console dependency auto-forward: `http`, `https`, `ws`, and `wss` localhost URLs, duplicate suppression, proxy-local-port skip, manual/auto forwards, hidden-console queue flush, noisy logs, and workspace switching.
8. Smoke test terminal preview-port detection: URL patterns, listening/running/server text patterns, duplicate suppression, detected-port rows, Browser manual/auto open flow, and noisy output while switching workspaces.
9. Smoke test Explorer behavior, profile behavior, workspace lifecycle, Browser console/forwarding behavior, terminal pane map sites, and Editor/Image/Notes lifecycle under workspace switching.
10. If lag remains, profile the real Windows runtime to separate Explorer DOM/cache-prune/backend polling/path helpers/typeahead/entry lookup, terminal write/scan/fit/layout/active-state/cwd parsing, Browser iframe/console/frame/sizing/device/tab/proxy/forward lookup, workspace-store/profile persistence, and tab/control render work.

## Commands
```bash
npm run check
npm run build
git diff --check
rustfmt src-tauri/src/lib.rs
npm run tauri -- build --no-bundle
```

## Last Test Result
- Command: `npm run check`
  - Result: pass.
- Command: `npm run build`
  - Result: pass; main app chunk 268.03 kB minified, CSS 40.13 kB minified, xterm lazy chunk 289.71 kB minified.
- Command: `git diff --check`
  - Result: pass.
- Command: `rustfmt src-tauri/src/lib.rs`
  - Result: blocked in WSL; `rustfmt` not found.
- Command: `npm run tauri -- build --no-bundle`
  - Result: blocked in WSL; `cargo` not installed, `cargo metadata` cannot run.

## Constraints
- Do not revert unrelated or user changes.
- Preserve all existing features while optimizing perceived performance.
- Keep Windows-first runtime behavior.
- Avoid committing secrets, tokens, raw local usernames, private URLs, or real home paths.
- Keep docs and handoff snapshots compact and privacy-safe.

## Resume Instructions
- Start by reading this file and `codex.md`.
- Verify actual repo state before editing: `git status --short`, `git diff --stat`, and relevant functions in `src/main.ts`.
- Trust repo state over this snapshot if they differ.
- Continue from `Next Actions`; do not mark the goal complete until Windows runtime behavior is validated.

## Unknowns
- Whether the full optimization stack eliminates perceived lag in the actual Windows-built app.
- Whether Workspace image store loop compaction preserves every workspace image store/load/save edge case.
- Whether Workspace restore tab loop helpers preserve every workspace restore/switch edge case.
- Whether Explorer visible lookup map trim preserves every Explorer selection/lookup/drop/typeahead edge case.
- Whether Explorer row render signature precompute preserves every Explorer row/render/name edge case.
- Whether Panel interaction DOM query trim preserves every panel focus/resize/snap/control/launcher edge case.
- Whether Terminal tilde cwd expansion allocation trim preserves every tilde prompt-cwd edge case.
- Whether Terminal OSC7 cwd scan exec loop preserves every OSC7 cwd edge case.
- Whether Browser console local-port scan exec loop preserves every dependency auto-forward edge case.
- Whether Terminal prompt cwd tail scan preserves every prompt-cwd edge case.
- Whether Terminal port scan allocation guard preserves every local preview detection edge case.
- Whether Explorer entry lookup cache covers every root-load/child-expand/refresh/delete/rename/runtime-restore/workspace-switch edge case.
- Whether Connection profile lookup cache covers every initial-load/WSL-merge/profile-select/workspace-restore/terminal-profile edge case.
- Whether Workspace snapshot lookup cache covers every load/create/copy/close/save/reorder/workspace-switch edge case.
- Whether Browser console row reuse covers every show/hide/clear/hidden-replay/workspace-switch edge case.
- Whether Forward/detected-port lookup caches cover every manual/auto/detected/workspace-switch edge case.
- Whether Editor/Image/Notes tab lookup caches cover every restore/create/open/close/rename/paste/hydration/workspace-switch edge case.
- Whether Browser device, preview-proxy, Browser tab, terminal-widget, workspace terminal snapshot, UI signature, workspace persistence, terminal scan/write, hidden console replay, and Explorer watcher/prefetch patches preserve every runtime edge case under real interaction.
